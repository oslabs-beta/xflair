import {useEffect, useRef, useState} from 'react';


export default function Top5 () {
    const [top5, setTop5] = useState({});
    const top5Probabilities = () => {
       const python5 = {/*top5 object goes here */}
       setTop5(p)thon5
    }
    
    return <img
    className = "resizedImage"
     src = {resizedImageURL}
     alt = "resizedImage"
     />;
}